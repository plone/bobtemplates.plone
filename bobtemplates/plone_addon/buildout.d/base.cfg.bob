[buildout]
package-name = {{{ package.namespace }}}.{{{ package.name }}}
package-extras = [test]

extends =
    http://dist.plone.org/release/{{{ plone.version }}}/versions.cfg
    checkouts.cfg
    sources.cfg
    versions.cfg

develop = .

eggs +=
	Pillow
	ipdb
