[buildout]
package-name = {{{ package.namespace }}}.{{{ package.name }}}
package-extras = [test]

extends =
    https://raw.github.com/collective/buildout.plonetest/master/plone-{{{ plone.version }}}.cfg
    checkouts.cfg
    sources.cfg
    versions.cfg

develop = .

eggs +=
	Pillow
	ipdb
