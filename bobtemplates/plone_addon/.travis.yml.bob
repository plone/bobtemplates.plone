language: python
python:
  - {{{ python.version }}}
install:
  - mkdir -p buildout-cache/{eggs,downloads}
  - python bootstrap.py -c travis.cfg
  - bin/buildout -Nc travis.cfg
script:
  - bin/test
notifications:
  {{{ travis.notifications.type }}}:
    - {{{ travis.notifications.destination }}}
